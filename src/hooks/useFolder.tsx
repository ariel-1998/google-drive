import React, { useEffect } from "react";

type useFolderProps = {
  folderId: string;
};

const useFolder: React.FC<useFolderProps> = ({ folderId }) => {
  // useEffect(())
  return <div>{/* Component content */}</div>;
};

export default useFolder;
